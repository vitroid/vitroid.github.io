# 論文追加

Paperpile のエクスポートから、短名（例: `MT2022`）付きの論文記事を増やす手順。  
（このファイルはリポジトリ用メモ。GitHub Pages には出さない。）

## 前提

- リポジトリ直下に `.env`（gitignore 済み）:
  ```
  GEMINI_API_KEY=...
  ```
- 初回のみ仮想環境:
  ```bash
  python3 -m venv MD/.venv-papers
  MD/.venv-papers/bin/pip install -r MD/requirements-papers.txt
  ```

## 短名ルール

- 著者姓の頭文字（著者順）+ 西暦年（例: Matsumoto & Tanaka 2022 → `MT2022`）
- 日本語姓: 松本→`M`, 田中→`T`, 二井矢→`N`, 河原→`K`
- 同年同順で衝突したら接尾辞 `A`, `B`, …（大文字）
- 既に `MD/{短名}.md` があるものは優先（上書きしない）
- 正本は [`MD/paper_ids.yaml`](MD/paper_ids.yaml)。一度付いた短名は JSON を差し替えても変えない

## いつもの手順（論文が増えたとき）

1. Paperpile から JSON をエクスポートし、リポジトリ直下に置く  
   （ファイル名例: `Paperpile - References - Mar 01.json`）

2. 短名を増分割当て（既存 id は維持）:
   ```bash
   MD/.venv-papers/bin/python MD/assign_paper_ids.py \
     --json "Paperpile - References - Mar 01.json"
   ```
   必要なら `MD/paper_ids.yaml` を手で直す。

3. 未作成の記事だけ生成（既存 MD はスキップ）:
   ```bash
   MD/.venv-papers/bin/python MD/gen_paper_pages.py \
     --json "Paperpile - References - Mar 01.json"
   ```
   - 1件だけ: `--id TMY2027`
   - 既存を書き直す: `--force`
   - 目録だけ更新: `--update-index-only`

4. 生成された `MD/{短名}.md` を目視で直す（高校生向け解説・タグ・カバー画像など）。

5. **サイト用 `_wiki/` を更新してから push**（ここを忘れると Pages に出ない）:
   ```bash
   cd MD && make parse
   git add MD _wiki _includes
   git commit -m "Add paper pages"
   git push
   ```
   Jekyll / Pages が読むのは `MD/` ではなく `_wiki/`。`MD/` は `_config.yml` で exclude されている。

## 生成されるもの

| 出力 | 内容 |
|------|------|
| `MD/paper_ids.yaml` | 短名・citekey・doi・Paperpile id の対応 |
| `MD/{短名}.md` | 書誌・解説・Abstract・タグ（ソース） |
| `_wiki/{短名}.md` | `make parse` 後の公開用ページ |
| `MD/papers.md` | `#paperYYYY` への短名追記 |
| `MD/paperYYYY.md` | 年タグ用スタブ（無ければ作成） |

`--json` を省略すると、リポジトリ直下の最新 `Paperpile*.json` を使う。

## スクリプト

- [`MD/assign_paper_ids.py`](MD/assign_paper_ids.py) … 短名割当て
- [`MD/gen_paper_pages.py`](MD/gen_paper_pages.py) … Gemini で記事生成
- [`MD/requirements-papers.txt`](MD/requirements-papers.txt) … 依存パッケージ

突合の優先順位: **doi → Paperpile `_id` → citekey**。JSON から消えた論文は yaml から削除せず `missing_in_source: true` になる。

## 補足

- Gemini 無料枠にはリクエスト上限がある。まとめて生成して失敗したら、時間をおいて同じコマンドを再実行すれば足りない分だけ作れる（既存はスキップ）。
- モデル既定は `gemini-3.6-flash`（`--model` で変更可）。
- `.env` と `MD/.venv-papers/` は git に含めない。
